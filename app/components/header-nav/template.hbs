<div class="svg-toolbox" data-grunticon-embed></div>
<ul class="js-site-nav site-nav">
    {{#each item in navItems}}
    <li>
        {{nav-icon title=item.title action=item.action route=item.route}}
    </li>
    {{/each}}
    <!--<li>
        <a {{action "toggleSubNav"}}>
            <svg role="img" class="svgico">
                <use xlink:href="/assets/img/svg/sprite.svg#toolbox"></use>
            </svg>
            <span>SUB NAV</span>
        </a>
    </li>-->
</ul>

{{#if showSubNav}}
<div class="site-sub-nav">
    {{header-nav navItems=navSubItems}}
</div>
{{/if}}


{{yield}}
